# Backend WebSocket URL
PUBLIC_BACKEND_WS_URL=ws://localhost:8787

# Sentry (DSN in sentry.client.config.js + sentry.server.config.js)
# SENTRY_AUTH_TOKEN — CI/CD secret only

# PostHog (public key, EU host hardcoded in src/components/posthog.astro)
PUBLIC_POSTHOG_KEY=
