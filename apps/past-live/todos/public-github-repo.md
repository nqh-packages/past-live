# Public GitHub Repo Strategy

Currently in private monorepo (`/Volumes/BIWIN/CODES/astro/`). Need public repo for hackathon.

## Options

- Extract `apps/past-live/` + `apps/past-live/server/` to standalone public repo
- Or make monorepo public (exposes other apps)
- Need to sanitize: remove .env files, secrets, internal references
