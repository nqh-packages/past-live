# README + Spin-Up Instructions

Hackathon submission requirement. Must demonstrate reproducibility.

## Requirements

- Clear setup instructions for judges
- Environment variables documented
- Both frontend (Cloudflare Workers) and backend (Cloud Run) setup
- Example `.env` files
- `pnpm install && pnpm dev` should work locally
