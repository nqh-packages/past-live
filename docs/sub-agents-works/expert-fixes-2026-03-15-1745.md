# Past, Live — Agent Context (2026-03-15, Expert + Judge Fixes)

## Topic

Fix 11 blockers + 4 majors from expert panel, plus privacy page and content blocklist from judges panel.

## What is Past, Live?

Voice-first educational app where students call historical figures via Gemini Live API. Hackathon submission for Gemini Live Agent Challenge (deadline March 16, 2026 @ 8pm EDT).

## Governance Files (MUST READ)

| File | What it contains |
|------|-----------------|
| `apps/past-live/CLAUDE.md` | All decisions, architecture, stack, gotchas |
| `/Volumes/BIWIN/CODES/astro/CLAUDE.md` | Monorepo conventions |

## Stack

| Layer | Technology |
|-------|-----------|
| Frontend | Astro 5 + Svelte 5 |
| Styling | Tailwind CSS v4 (`@theme {}` tokens in `global.css`) |
| Islands | Svelte 5 runes (`$state`, `$derived`, `$effect`, `$props`) |
| Auth | Clerk (`@clerk/astro`) |

## Design Conventions

- Terminal/dispatch aesthetic: `>` prefix, `[ brackets ]` for CTAs, monospace
- Error copy stays in phone metaphor: "SIGNAL LOST", "the past is not responding"
- `font-mono` for UI, `font-serif italic` for headlines, `font-display` for accent
- Dark theme: near-black background, muted foreground, red accent

## Recent Changes (already committed)

- `SessionPreview.svelte:52`: mic default changed to `$state(false)`
- `SessionPreview.svelte:179-185`: error messages mapped to in-character copy
- `SessionPreview.svelte:379`: header changed from "calling" to "incoming"
- `SessionPreviewBody.svelte:230,232`: mic label aligned
- `app.astro:88`: `client:load` → `client:only="svelte"` (hydration fix)
- `app.astro:127-131`: footer revised with AI disclaimer
- `session.astro:26-27`: fake scenario ID validation + redirect
- `BaseLayout.astro:32-35`: OG image meta tags added
- `index.astro:36`: "no wrong answers" → "no wrong questions"
